version: "3.8"
services:
  db:
    image: postgres
    container_name: spring_pg
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: spring_db
    ports:
      - "5450:5432"